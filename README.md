# knpwrs Tap

## How do I install these formulae?

`brew install knpwrs/tap/<formula>`

Or `brew tap knpwrs/tap` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
